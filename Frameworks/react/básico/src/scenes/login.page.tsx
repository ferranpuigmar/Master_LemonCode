import { CenterLayout } from "@src/layouts";
import { LoginContainer } from "@src/pods/login";

export const LoginPage = () => {
  return (
    <CenterLayout>
      <LoginContainer />
    </CenterLayout>
  );
};
